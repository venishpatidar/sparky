class Config:
    def __init__(self):
        # tokenizer and weight path (either used the pretrained or new one) 
        self.TOKENIZER_PATH = "./T5-fine-tuned-NLP-JSON"
        self.MOEDL_PATH = "./T5-fine-tuned-NLP-JSON"

config = Config()
